Этот файл — точка входа для продолжения разработки SmartBESS EMS в новой сессии (после обрыва/потери контекста). Второй такой файл — [MEMORY.md](MEMORY.md) в этой же папке: там фичи/UI/эксперименты с прогнозом/планы/рыночное исследование. Читать оба перед началом работы.

**Эти два файла должны обновляться при каждом значимом изменении проекта** (новая/изменённая фича, решение по расчёту, изменение UI-конвенции, проверенный вариант прогноза, деплой, план) — не откладывать до следующей сессии. После каждого `git commit` в `.claude/settings.json` (project-level, при cwd внутри `smartbess-ems`) настроен `PostToolUse`-хук на `Bash`, который добавляет в контекст напоминание проверить/обновить `CLAUDE.md`/`MEMORY.md`. Хук только напоминает — реально переписывает файлы каждый раз ассистент, не автоматика; если изменение не документируемое, напоминание можно просто проигнорировать.

## Что это

SmartBESS EMS — платформа для украинского BESS-накопителя, зарабатывающего на арбитраже цен рынка на добу наперед (РДН/DAM): прогноз цены (ML) → MILP-оптимизация графика заряд/разряд → диспетчеризация → SCADA-симуляция → финансовая отчётность для инвестора. Домен: Україна, `oree.com.ua` (Оператор ринку) как основной источник цен.

## Стек и структура

- Backend: FastAPI + SQLAlchemy, `app.py` — вход, `uvicorn app:app`, порт 5000 по умолчанию.
- Frontend: React 19 + Vite + TS в `frontend/`, роли Viewer/Operator/Manager/Admin, два воркспейса: **Dispatcher Console** (оперативная работа) и **Director Dashboard** (инвестор/руководитель).
- БД: sqlite локально (`data/smartbess.db`), Postgres/TimescaleDB в docker-compose для прод-подобного окружения. Схема — `Base.metadata.create_all` (`src/database/init_db.py`), без Alembic — новая модель применяется просто рестартом приложения.
- Ключевые модули backend:
  - `src/modules/forecast_service/ml_pipeline.py` — прогноз цены (LightGBM/XGBoost/MLP + LightGBM quantile P10/P90).
  - `src/modules/optimization_service/milp_model.py` — PuLP/CBC MILP-оптимизация графика батареї + Monte Carlo VaR.
  - `src/modules/market_data_service/data_manager.py` — сбор/кэш реальных данных (`oree.com.ua`, Open-Meteo).
  - `src/modules/external_data_service/` — `entsoe.py` (трансграничные перетоки + цена соседних зон), `intraday_market.py` (ВДР/IDM с oree), `gas_price.py` (index.minfin.com.ua), `telegram_public.py`/`telegram_bot.py` (публичные посты Укренерго/Мінэнерго как сигнал грид-стресса + алерты, теперь также нагадування по заявках), `neighbor_weather.py` (погода PL/RO).
  - `src/modules/scada_service/` — симулятор Modbus-телеметрии + `soc_state.py` (цепочка приоритетов для стартового SoC).
  - `src/modules/bidding_service/services.py` — реальний механізм подачі заявок РДН (аукціон єдиної ціни), легальні межі ціни OREE, синтезований «центр дій» диспетчеру (`build_daily_action_summary`).
  - `src/tasks/scheduler.py` — ежедневная джоба 06:00 (перенесена с 17:30 2026-08-04 — см. хронология): sync данных → прогноз (LightGBM) → P10/P90 → MILP → сохранение плана. Это чистый инференс — модель НЕ переобучается этой джобой, используется уже обученный `.pkl` с диска. Отдельная ночная джоба (02:00, п.15) переобучает модель. Новая джоба 10:00 (п.18) — Telegram-нагадування про заявки.

## Как запускать/тестировать локально (грабли уже найдены — не наступать заново)

- Бэкенд: системный `python3` (fastapi/sqlalchemy стоят через `pip --user`). **`.venv` в репозитории НЕ имеет нужных зависимостей** — не использовать.
- Порт 5000 часто занят посторонним процессом (`~/energo/energy-prediction-ua`, другой проект) — проверять `ss -ltnp` перед тем как считать что-то этим репо; для ad-hoc тестов бэкенда использовать другой порт (напр. 5050).
- Frontend dev-сервер (`npm run dev`/vite) в этом WSL-окружении не поднимается: `node_modules` собран под Linux, но реально доступен только Windows `node.exe`. Это давняя проблема окружения, не чинить переустановкой node_modules без явного запроса.
- **Проверка TS перед деплоем — только `tsc -b`, НЕ `tsc --noEmit`.** Проект использует составной tsconfig (`tsconfig.json` + `tsconfig.app.json`/`tsconfig.node.json` через project references), и реальная сборка (`npm run build` → `tsc -b && vite build`) применяет более строгие настройки, чем плоский `tsc --noEmit` по корневому конфигу. Найдено на практике 2026-07-31: `tsc --noEmit` прошёл чисто, а `docker compose build` на VPS упал с `TS18046 'r' is of type 'unknown'` (generic-функция без явного типа возврата резолвится в `unknown`, а не в `any`, в строгом режиме). Проверять командой `"/mnt/c/Program Files/nodejs/node.exe" node_modules/typescript/bin/tsc -b --force` (`--force` — чтобы не полагаться на инкрементальний кэш) — только она реально соответствует продовой сборке. **Ещё одна ловушка, найдена 2026-08-06 (п.18)**: `lucide-react`-іконки не приймають `title`-проп напряму (`Property 'title' does not exist on type ...LucideProps`) — обгортати в `<span title="...">`, якщо потрібен нативний tooltip на іконці.
- Авторизация — мок unsigned JWT (`alg: none`), собирается на клиенте (`frontend/src/api/client.ts::getMockToken`), роль — из claim `roles`. Для curl легко подделать base64url-скриптом.
- В коде/UI намеренно используется украинский язык (комментарии, докстринги, строки интерфейса) — это принятая конвенция проекта, не ошибка и не то, что нужно "исправлять".
- **`src/database/init_db.py` сам не импортирует `src.database.models`** — если писать standalone-скрипт (не через `app.py`), который вызывает `init_db()` напрямую, нужен явный `import src.database.models` ПЕРЕД вызовом `init_db()`, иначе новые ORM-классы не зарегистрированы в `Base.metadata` и `create_all` их не создаст (найдено 2026-08-06, п.18 — локальная sqlite не имела таблиц `market_bids`/`bid_margin_overrides`/`price_shift_overrides`, т.к. эти фичи разрабатывались сразу на VPS).

## Главный принцип проекта: никаких выдуманных данных

Явное правило по всему коду ("не вигадувати", "не приймається на віру", "чесно NaN"): реальные данные и честный NaN на пробелах — никогда не константа-затычка или `np.random`. Раньше почти все "рыночные факторы" (Nuclear_Gen, Gas_Price, VDR_Volume, Grid_Import_Export и т.д.) были синтетическим шумом — это была главная причина плохого прогноза, всё заменено на реальные источники (см. MEMORY.md, раздел "Источники данных"). Новая фича идёт в продовый `FEATURES` только после `walk_forward_backtest`, и результат (даже отрицательный) документируется комментарием в коде, а не тихо выбрасывается.

## Продакшн-модель прогноза

Основная модель — **LightGBM** (`scheduler.py` всегда использует `predictions['lightgbm']`; XGBoost/MLP считаются параллельно и доступны в ручном запуске `/forecast/run` через `selected_model`, но не являются продовым выбором). P10/P90-интервал — отдельная LightGBM quantile-регрессия со split-conformal калибровкою на последних 60 днях (заменил вымышленное допущение ±1.64σ в риск-сценариях MILP). Подробности по фичам/экспериментам — MEMORY.md.

## Деплой / VPS

Актуальные детали (адрес, SSH, docker-layout, процедура hot-patch vs полная пересборка образа, история инцидента с ransomware, лимиты памяти) хранятся в auto-memory Claude Code для этого проекта: `/home/oleg/.claude/projects/-home-oleg-claude-smartbess-ems/memory/` (файлы `reference_vps_deployment.md`, `project_db_ransomware_incident.md`, `feedback_vps_resource_constraints.md`) — эта память привязана к конкретному окружению/машине и не хранится в git, поэтому если работаешь с чистого листа без доступа к ней, спроси пользователя актуальный адрес/доступ VPS заново, не полагайся на устаревшие числа ниже.

Коротко (может устареть, сверяться с memory выше):
- Тестовый деплой: `http://olegkg2016.vps.ho.ua:5004`, SSH только как `root`, ключ уже в окружении.
- `src/` в образе (`COPY . .`), НЕ bind-mounted — правка файла на хосте сама по себе ничего не меняет в контейнере; нужен либо `docker cp` + `docker restart` (быстрый hot-patch) либо `docker compose build && up -d` (постоянно). После деплоя проверять не "импорт не падает", а реальный запрос к конкретной фиче (см. случай с Excel-экспортом ниже).
- Git remote `origin` = `github.com/Olegkg2018/smartbess-ems.git`. В песочнице обычно нет push-credentials — пуш делает пользователь сам либо даёт одноразовый токен.
- 2026-07-15/16: VPS был скомпрометирован (Postgres с дефолтными кредами, опубликован на 0.0.0.0:5432) — автоматический ransomware-бот снёс БД. Реальные данные не пострадали (CSV-кэш вне докер-волюма), реплика восстановлена 2026-07-20, добавлен ежедневный backup. Не считать "не работает на VPS" автоматически багом кода — сначала проверить экспозицию портов.
- VPS: 2GB RAM (апгрейд с 1GB 2026-07-30), swap нет. **С 2026-08-04 обучение моделей на VPS разрешено и автоматизировано** (см. хронология п.15) — живой тест показал реальный пик памяти ≈598MB из лимита 900m (было ошибочно оценено ≈856MB), ночная джоба `run_nightly_model_retrain` (02:00, `src/tasks/scheduler.py`) сама переобучает и точечную, и quantile-модель. Старая политика «никогда не обучать на VPS» (2026-07-20 — 2026-08-04) была верна для тех условий (1GB RAM/лимит 450m), но устарела — не полагаться на неё не проверив текущие числа заново. Ручной `POST /api/retrain` по-прежнему блокирует event loop FastAPI на время обучения (это архитектурное, не памятное ограничение) — для регулярного обучения использовать ночную джобу, а не этот эндпоинт.

## Хронология выполненных доработок (по коммитам)

1. **adace31** (2026-07-09) — начальный коммит: платформа целиком сгенерирована (TimescaleDB, MILP-оптимизатор, React/TS фронтенд), но с фейковыми рыночными факторами и монолитним `App.tsx`.
2. **8a1cd31** (2026-07-10) — «Фаза 1/2»: все синтетические факторы заменены реальными источниками (oree DAM+IDM, Open-Meteo, ENTSO-E перетоки, реальная цена газа, публичные Telegram-сигналы, физическая модель Solar_Gen/Wind_Gen из погоды); добавлен P10/P90 quantile-прогноз с conformal-калибровкой; честный walk-forward backtest (WAPE) вместо одного holdout-сплита; фронтенд разделён на Dispatcher Console / Director Dashboard с react-router; исправлены реальные баги (job_id NameError, недостающая персистенция PriceForecast, рассинхрон Settings/Asset). Проверенные и **отклонённые** по бэктесту эксперименты (recency weighting, two-stage surplus classifier, price-cap clipping, EU DAM price фича) задокументированы в коде, не выброшены молча.
3. **ab2f612** (2026-07-13) — исправлены баги непрерывности SoC (симулятор игнорировал реальные параметры Asset; стартовый SoC был захардкожен на 20% каждый день); добавлены `GenerationAdjustment` (ручная поправка диспетчера на доступность АЕС/ГЕС/СЕС/ВЕС, физически обоснованная, не произвольный коэффициент) и `InitialSocOverride`; объединён расчёт заряд/разряд/SoC в единый `AppContext.dispatchProfile`, чтобы страницы физически не могли разойтись; MAPE → WAPE в инсайтах (деление на почти нулевую цену давало 9000%+); валидирована цветовая палитра под dark-mode/CVD.
4. **6078de5** (2026-07-15) — пустой график для дат, где расчёт реально никогда не запускался, заменён явным сообщением с указанием на кнопку «Розрахувати» (диспетчер принимал это за баг).
5. **a6e9b01** (2026-07-30) — крупная сессия: исправлен train/serve skew самой влиятельной фичи модели `IDM_Price_Lag_24` (~55% gain) — на инференсе хвост восстанавливается через форму реальной цены РДН + медианную разницу ВДР-РДН, а не плоской экстраполяцией; добавлена страница Data Audit («что реально использовалось в расчёте на эту дату»); `GridStressOverride` — ручной фолбэк на сигнал грид-стресса; SoC теперь может брать «расчётный конец предыдущих суток» как третий уровень приоритета; `max_cycles_per_day` вынесен в настройки; добавлен экспорт в Excel (`/reports/export-day`); собрана экспериментальная погода PL/RO (не включена в FEATURES — см. MEMORY.md).
6. **(без отдельного коммита в этом sandbox, 2026-07-30 вечер)** — обнаружено и исправлено: примерно 11 backend-файлов и 4 frontend-файла из пункта 5 **никогда реально не доезжали до VPS** несмотря на то, что диф-проверка «всё совпадает» проходила зелёным — сама Excel-фича включая кнопку физически отсутствовала в проде. Причина ложноположительной проверки не выяснена до конца (подозрение на баг батч-цикла ssh+diff). Файл-за-файлом передеплоено, подтверждено реальным запросом (`.xlsx` действительно приходит, HTTP 200), закоммичено и запушено в `origin/main`. **Урок, зафиксированный в памяти**: после любого деплоя проверять не «импорт не падает», а реальный пользовательский сценарий.
7. **(2026-07-31, коммит `7029a69`, запушено в `origin/main`)** — реализованы и честно проверены две идеи из рыночного исследования (MEMORY.md §6a): (a) `milp_model.py::optimize_battery_schedule_dam_idm` — РДН+ВДР co-оптимизация, оракульный бектест на 300 реальных днях показал прирост всего +0.4-1.2%, к продовому диспетчеру НЕ подключено (см. MEMORY.md); (b) `forecast_accuracy.py::compute_real_profit_capture_ratio` — честный финансовый "% от идеального прогноза" (реальный P&L диспетчеризации / perfect-foresight MILP), заменяет WAPE-проксі первым в приоритете `get_profit_capture_ratio`, с защитой от несовпадения лимитов актива между старым планом и текущими настройками. Фронтенд (`ForecastAccuracy.tsx`) обновлён под новый источник.

8. **(2026-07-31, коммит `7029a69` + фикс типов сверху, задеплоено на VPS и проверено реальными запросами)** — реализован реальный механизм подачи заявок РДН (не гипотеза "исполняется всегда по прогнозу", а честная модель аукциона единой цены): новые таблицы `MarketBid` (заявка/факт/статус исполнения/реализованный P&L/предложение ВДР) и `BidMarginOverride` (ручная маржа диспетчера на сутки — по аналогии с `InitialSocOverride`); модуль `src/modules/bidding_service/services.py` (`generate_bids_for_date`, `settle_bids_for_date`); новый роутер `/api/v1/bids` (margin GET/POST/DELETE, generate, settle, list); `ml_pipeline.py::estimate_idm_price_for_hour` — оценка цены ВДР для предложения, когда заявка РДН не сыграла. Фронтенд: новая карточка «Заявка РДН» на Optimization Schedule (ввод маржи рядом с SoC, таблица по часам: прогноз/цена заявки/факт/статус/P&L или ВДР-предложение). **Автоматическая подача через API в кабинет OREE НЕ реализована** (явно за рамками этой сессии, по словам пользователя — "в будущем") — сегодня диспетчер вручную переносит показанные цифры в реальный кабинет oree.com.ua.
   - **При деплое на VPS (`docker compose build`) сборка упала** — `tsc -b` (реальная продовая проверка типов) поймал `TS18046` в `client.ts::generateBids/settleBids` (generic без явного типа возврата резолвился в `unknown`). Исправлено явными интерфейсами `GenerateBidsResult`/`SettleBidsResult` — см. урок про `tsc -b` выше.
   - **Проверено на VPS реальными запросами** (не просто "нет ошибок в логах"): `profit_capture_ratio` вернул `source: "real_dispatch_vs_perfect_foresight"`, ratio 93% за 29 реальных дней (442684/476021 грн). `/bids/generate` + `/bids/settle` на реальную дату (2026-07-29) — 24/24 заявки исполнены при дефолтной марже 2%, total_realized_profit_uah 21735 грн. Таблицы `market_bids`/`bid_margin_overrides` подтверждены в Postgres.
   - **Побочно обнаружено**: git HEAD на VPS отставал на 3 коммита от `origin/main` (застряв на `ab2f612`), хотя содержимое файлов на диске уже соответствовало новым коммитам (результат прошлых hot-patch через `scp` без `git commit` на самой VPS) — задеплоенные сегодня файлы сверены построчно с `origin/main` перед копированием, не пакетным диффом.
9. **(2026-07-31, продолжение сессии — коммиты `3ea6ee4`/`49998d2`, запушено в `origin/main` как `7f37dd6`/`12243f9`, задеплоено на VPS и проверено реальным запросом)**:
   - (a) `compute_real_profit_capture_ratio` (`forecast_accuracy.py`) теперь для суток с полностью звiренными `MarketBid` считает "actual" через `MarketBid.realized_profit_uah` (честный ноль за часы, где заявка РДН не сыграла), а не через `ChargeDischargePlan` × факт-цену (неявно предполагает 100%-исполнение) — фолбэк на старый метод остаётся для суток до появления механизма заявок. Новые поля `n_days_market_bid_settled`/`n_days_plan_full_execution_assumed` в API и на `ForecastAccuracy.tsx`. См. MEMORY.md §1a.
   - (b) Бэктест блендинга LightGBM+XGBoost+MLP (`ensemble_average`/`ensemble_weighted`, `scratch/backtest_ensemble.py`) — сигнал по `last_7d`/`last_30d` нестабилен и разнонаправлен между схемами, **отклонено от прода** (solo LightGBM остаётся). Код (`ENSEMBLE_MODEL_TYPES` в `ml_pipeline.py`) оставлен для повторной проверки на большем `test_days`. См. MEMORY.md §4, §6a.
   - GitHub MCP-сервер (окружение Claude Code, не репозиторий): OAuth подтверждён в этой сессии — браузерный логин прошёл, `get_me` вернул профиль `Olegkg2018`.
   - Локальный `git push` не сработал в этой песочнице (нет credentials) — все 3 коммита сессии запушены через `mcp__github__push_files` (GitHub REST API), файл за файлом, с сохранением порядка/сообщений коммитов. Побочный эффект: локальные и удалённые SHA разошлись при идентичном содержимом — синхронизировано `git reset --hard origin/main`. **Если push через GitHub MCP используется снова и после него нужно продолжать коммитить локально — не забыть `git fetch && git reset --hard origin/main` перед следующим коммитом**, иначе локальная история разъедется дальше.
   - **Задеплоено на VPS и проверено реальным запросом**: диф host-path VPS vs локальных файлов (не пакетный, файл за файлом) нашёл реальные различия в 3 файлах (`forecast_accuracy.py`, `ForecastAccuracy.tsx`, `ml_pipeline.py`) — `scp` + `docker compose build && up -d`. `GET /api/v1/reports/forecast-accuracy` вернул `n_days_market_bid_settled: 1`, `n_days_plan_full_execution_assumed: 29`, ratio 92.65% за 30 дней — подтверждает, что новая ветка кода реально исполняется в проде, а не просто "не падает при импорте".
10. **(2026-08-03)** — расследован и исправлен реальный баг: диспетчер сохранил `GenerationAdjustment` (ручна корекція АЕС/ГЕС/СЕС/ВЕС) на дату с сильным расхождением прогноза и реальной цены, ожидая сдвига прогноза — эффекта не было. Найдены и устранены две независимые причины:
   - (a) **Workflow-разрыв**: `POST /generation-adjustments` был чистой записью в БД без побочного пересчёта `PriceForecast`. Теперь эндпоинт (`src/api/v1/endpoints/generation_adjustments.py`) сам проверяет, существует ли уже прогноз на эту дату, и если да — ставит в фон тот же `run_forecast_background_job`, что и кнопка «Зберегти і перерахувати прогноз» (переиспользован, не продублирован), возвращая `recompute_triggered`/`recompute_job_id`.
   - (b) **Насыщение модели**: по реальному `feature_importance` продовой `data/model_lightgbm.pkl` реагирующие на корекцію фичи (`Solar_Gen`/`Wind_Gen`/`Grid_Net_Export_*`) суммарно дают <2% веса модели (~92% — авторегрессионные `IDM_Price_Lag_24`/`Price_Lag_*`), а справочные потужности АЕС/ГЕС (7835/3800 МВт) на порядок больше реального разброса `Grid_Net_Export_MW` (std≈392 МВт) — уже при 20-30% отклонения дельта выходила за пределы обучающего распределения LightGBM (дерева не экстраполируют) и прогноз переставал реагировать. Исправлено в `ml_pipeline.py::build_forecast_feature_matrix`: новый явный коэффициент `baseload_passthrough_ratio` (default 0.3, редактируется в Settings как и справочные потужности) + жёсткий clip дельты по реальному 99-перцентилю `Grid_Net_Export_MW` за последние 180 дней (не выдуманное число — считается из уже загруженных исторических данных). Строгий no-op при *_pct=100 (нейтральное значение) — проверено структурно.
   - **Остаточное честное ограничение** (не решается калибровкой): даже после фикса реагирующие на `GenerationAdjustment` пути дают <2% веса модели — заметный сдвиг прогноза одной лишь этой поправкой архитектурно невозможен без настоящего признака генерации по типам, которого нет и не будет, пока ENTSO-E не возобновит публикацию по Украине (остановлена 25.02.2022). Текст подсказки в `PriceForecast.tsx` дополнен (не переписан) явным предупреждением об этом.
   - **Задеплоено на VPS и проверено реальным запросом (2026-08-03)**: hot-patch + полная пересборка образа (`docker compose build && up -d`). `GET /api/v1/optimization/settings` вернул `baseload_passthrough_ratio: 0.3`; `POST /api/v1/generation-adjustments` для даты с уже существующим `PriceForecast` вернул `recompute_triggered: true`; реальный пересчёт сдвинул среднюю прогнозную цену с 5189.47 до 5327.54 грн/МВт·год при 70% доступности АЕС и корректно вернулся к 5189.47 после сброса на 100%. Текст в собранном JS-бандле фронтенда подтверждён напрямую (`curl` по реальному ассету).
11. **(2026-08-03, продолжение сессии)** — реализован ручной відсотковий зсув прогнозу ціни (`PriceShiftOverride`) — для случая, когда диспетчер знает о реальной временной рыночной аномалии (напр. игрок рынка коротко манипулировал/занижал цену, уже откачено), которую модель не могла предсказать и не должна учиться распознавать по одному эпизоду. Принципиально другой механизм, чем `GenerationAdjustment` (п.10): применяется **после** предсказания модели (`predict_next_day`/`predict_price_band` в `ml_pipeline.py`), а не как фича — clip(raw, FLOOR, CAP) → умножение на `(1 + shift_pct/100)` → повторный clip. Полностью обходит проблему насыщения деревьев (ни одна фича не меняется) и структурно не задевает `walk_forward_backtest`/`train_models` (они вызывают `model.predict()` напрямую на исторических данных — подтверждено, `_get_price_shift_pct` не вызывается больше нигде в файле кроме этих двух функций).
    - Новая таблица `PriceShiftOverride` (`date` PK, `shift_pct` default 0.0, `note`) в `src/database/models.py`. Новый роутер `src/api/v1/endpoints/price_shift.py` (`/api/v1/price-shift`, GET/POST), зарегистрирован в `src/api/v1/api.py`.
    - **Вынесен общий хелпер** `trigger_forecast_recompute_if_exists` (`forecast.py`) — та же логика авто-триггера пересчёта, что появилась для `GenerationAdjustment` в п.10, теперь используется обоими эндпоинтами (не дублируется второй раз — дублирование один раз уже привело к найденному в п.10 багу).
    - Фронтенд: новая карточка «Ручний зсув прогнозу ціни» на Price Forecast (после карточки корекції генерації), баннер в «Ключові спостереження», surfacing в Data Audit (`manual_overrides.price_shift`).
    - **Проверено локально**: `predict_next_day`/`predict_price_band` при `shift_pct=0.0` дают побайтово идентичный результат отсутствию оверрайда; при `shift_pct=-20.0` каждый час прогноза и P10/P90-полосы даёт ratio ровно `0.8000` к бейзлайну (проверено на реальных данных 2026-07-31); `lower<=upper` сохраняется. `tsc -b --force` прошёл чисто.
    - **Проверено реальным HTTP-циклом** (локальный сервер, порт 5050): `POST /forecast/run` → `POST /price-shift` (-20%) вернул `recompute_triggered: true` → после пересчёта `GET /forecast/latest` показал ratio к бейзлайну ровно 0.8 (5536.60 → 4429.28 грн/МВт·год) → `GET /data-audit` корректно отразил сохранённый оверрайд → сброс на 0% вернул прогноз к исходным 5536.60 побайтово точно.
    - **Задеплоено на VPS и проверено реальным HTTP-циклом**, закоммичено и запушено в `origin/main` (2026-08-03).
12. **(2026-08-03/04, продолжение сессии)** — переименована надпись «Маржа заявки (%)» → «Відсоток буфера безпеки (Safety Buffer %)» в `OptimizationSchedule.tsx` (только UI-текст, по прямому пожеланию пользователя — «мне кажется так правильней звучит»). `tsc -b --force` чисто, задеплоено на VPS (`docker compose build && up -d`), подтверждено `curl` по реальному собранному JS-бандлу (новый текст присутствует, старый — нет).
    - **Найден и устранён реальный инцидент при push через `mcp__github__push_files`**: при ручном наборе больших файлов с кириллицей в тело запроса возможны два разных по тяжести сбоя — (a) точечные опечатки/искажения слов в комментариях (напр. «Розрахувати» → «Rozraxuvaty», обнаружено при `git diff HEAD origin/main` после пуша `AppContext.tsx`/`DataAudit.tsx`); (b) при повторном пуше `OptimizationSchedule.tsx` содержимое было ошибочно реконструировано по памяти вместо свежего чтения файла — на `origin/main` чуть не улетела частично вымышленная версия компонента (149 вставок/169 удалений вместо ожидаемой 1 строки), поймано только обязательной пост-пуш проверкой `git diff --stat`. Оба случая исправлены повторным пушем содержимого, прочитанного заново с диска. **Новое железное правило зафиксировано в auto-memory**: перед любым `mcp__github__push_files` для файла, не прочитанного в текущей цепочке вызовов, — сначала обязательный свежий `Read`, никогда не реконструировать содержимое по памяти разговора. См. auto-memory `feedback_mcp_push_text_corruption_risk.md`.
    - Git полностью синхронизирован (`origin/main`, VPS, локальный working tree).

13. **(2026-08-04)** — по вопросу пользователя «как часто должна переобучаться модель, что делает джоба 17:30» расследовано и исправлено:
    - Джоба 17:30 оказалась чистым инференсом (`predict_next_day`/`predict_price_band` грузят уже обученный `.pkl`), автоматического переобучения модели **нигде в проекте не было вообще** — только ручной `POST /api/retrain` (Manager/Admin, `app.py`), синхронно вызывающий `train_models()`. Найден дополнительный пробел: этот эндпоинт переобучает только точечную модель, но не `train_quantile_models()` (P10/P90-полоса), которая вообще ничем не запускается в проде.
    - По просьбе пользователя джоба **перенесена с 17:30 на 06:00** (`src/tasks/scheduler.py::start_scheduler`) — целевая дата (`tomorrow = today + 1`) не менялась, это чистый перенос времени триггера. Причина: реальная цена РДН на целевую дату публикуется только после ~13:00, а к 06:00 уже накопились более свежие данные погоды/Telegram за ночь, чем были доступны накануне в 17:30.
    - Проверена возможность переобучения на VPS (после апгрейда 1GB→2GB, 2026-07-30): живой `docker-compose.override.yml` на VPS содержит `mem_limit: 900m` для `smartbess-platform`, но собственный комментарий файла явно говорит, что этот запас — на обычные всплески (Excel-экспорт), а НЕ разрешение обучать ML в этом контейнере — решение «обучать локально, заливать `.pkl`» было принято осознанно и оставлено в силе несмотря на апгрейд RAM. **Эмпирически подтверждено**: локальный прогон `train_models()` на реальном `historical_data_merged.csv` (~49k строк) — пиковая память **426MB**, время **367.6с (~6.1 мин)**. `docker stats` на VPS в простое: `smartbess-platform` уже занимает **430.5MiB из лимита 900MiB**. Сумма ≈856.5MB — **<5% запаса** до жёсткого лимита без swap, плюс синхронный HTTP-запрос заблокировал бы живой диспетчерский API/SCADA-симулятор на 6+ минут. **Вывод: небезопасно** — живой тест на VPS НЕ проводился (не соответствует условию плана «явный запас»). Политика «обучать только локально, заливать `.pkl`» подтверждена и остаётся в силе; предупреждение об этом добавлено в докстринг `POST /api/retrain` (`app.py`). Расписанное автопереобучение (еженедельное, `train_models()`+`train_quantile_models()`) НЕ добавлено — потребует либо отдельного бокса для тренировки, либо асинхронной архитектуры вместо синхронного HTTP-хендлера, что выходит за рамки этой сессии.

14. **(2026-08-04, продолжение)** — по просьбе пользователя вручную переобучена и задеплоена модель («посмотрим как повлияет резкое изменение цены на рынке») — реальный повод: 2026-08-02/03/04 три дня подряд почти идентичный профіль, вечірні години кілька годин поспіль рівно 15000 грн (впритул до `PRICE_CAP=16000`), денний провал помітно вище звичного (10-3150 замість близько нуля) — та сама підозріла картина, що вже породила `PriceShiftOverride` (п.11).
    - Локально: бекап старих `.pkl` (`data/model_*.pkl.before_2026-08-04`), `dm.sync_realtime_data(force=True)` (дані вже сягали `2026-08-04 23:00`), `train_models()` (236.6с) + `train_quantile_models()` (10.8с, conformal correction 273.3 грн, coverage 80.07%).
    - **feature_importance майже не змінилась** (`IDM_Price_Lag_24` 47.40%→47.17%, `Price_Lag_24` 28.33%→28.68%, максимальний зсув 0.35пп) — один день на фоні 5.6 років даних фізично не може перебудувати дерева.
    - **Але прогноз на завтра (2026-08-05) зсунувся відчутно** — порівняно стару/нову модель на однакових вхідних даних: денний профіцит (11:00-14:00) **+20%...+36.5%** (13:00: 2438→3328 грн), вечірній пік трохи нижче (-1%...-3.8%), у середньому за добу +2.5%. Механізм: структура дерева та сама, але `Price_Lag_24`/`IDM_Price_Lag_24` (~76% ваги) буквально підставляють вчорашні (аномальні) числа — модель чесно екстраполює незвично високий вчорашній «підлоговий» рівень на завтра.
    - **Бэктест 14 днів (22.07-04.08, `walk_forward_backtest(test_days=14, retrain_every_days=7)`)**: `mean_wape=29.1%` (вище задокументованого бейзліну ~22-26%, але коротке волатильне літнє вікно, не пряме порівняння). Самі дні аномалії (2-4 серпня) WAPE 20.4%/**35.9%**/24.9% — 3 серпня гірше середнього, але НЕ найгірший день у вікні: найгірші — **25 та 27 липня (WAPE 62.5%/58.4%)**, тобто інше, раніше збурення ринку. MAPE на кількох днях «вибухає» до тисяч % — відомий артефакт ділення на майже нульову ціну (тому в проєкті всюди WAPE, не MAPE).
    - **Задеплоено на VPS**: `.pkl`+`conformal_calibration.json`+`metrics_report.json` scp'нуто напряму в `/root/energo/smartbess-ems/data/` (bind-mount, рестарт не потрібен — модель вантажиться з диска на кожен запит). Підтверджено реальним запитом: `GET /api/metrics` на VPS повернув саме нові цифри (`wape: 23.675`). **Важливий нюанс**: сьогоднішній `PriceForecast` на 2026-08-05, вже збережений ранковою джобою о 06:00, порахований ще СТАРОЮ моделлю (нова лягла о 08:41, вже після) — застосується автоматично тільки з завтрашнього запуску джоби или при ручному «Перерахувати».

15. **(2026-08-04, продолжение)** — пользователь спросил, почему прогноз 3-5.08 всё ещё расходится с фактом, раз модель уже переобучена (п.14), и предложил реально проверить обучение на VPS (память увеличили именно из-за прошлых падений) с идеей сделать переобучение ежедневным ночным. Расследование (read-only) показало: расхождение объясняется не качеством модели, а тем, что ни один из сохранённых прогнозов на 3/4/5.08 физически не мог использовать новую модель — джоба считает прогноз раз в сутки и всегда успевала посчитать раньше очередного ретрейна (3.08 и 4.08 — ещё по старому расписанию 17:30, 5.08 — в 06:00, на 2ч41м раньше деплоя модели в 08:41). Реальный WAPE тех дней (16.4%/17.3%/16.0% по `GET /forecast/latest` vs `GET /forecast/actual`) при этом даже лучше среднего 14-дневного бэктеста (29.1%) — ощущение "не совпадает" в основном из абсолютных грн на аномальных часах, а не из процента.
    - **Живой тест обучения прямо в контейнере VPS** (`docker exec`, не через `/api/retrain` — чтобы не блокировать event loop): baseline памяти 291.9MiB, полный цикл `sync_realtime_data(force=True)`+`train_models()`+`train_quantile_models()` — **пик всего 597.9MiB из лимита 900MiB (66%, ≈300MB реального запаса)**, что заметно безопаснее прежней консервативной оценки (≈856MB, п.13). `train_models()` в контейнере занял 132.1с (быстрее локальных 236.6с), `train_quantile_models()` — 13.8с, `sync_realtime_data` — 673.6с (доминирует, внешние API), итого ≈13.7 мин. API отвечал 80-120мс и во время, и после теста; контейнер ни разу не перезапускался (`StartedAt` не изменился).
    - **Найден и устранён реальный риск для автоматизации без присмотра**: `train_models()`/`train_quantile_models()` писали `.pkl`/`.json` напрямую поверх боевого файла (`open(path, 'wb')`) — если процесс убьют (OOM/рестарт) ровно во время записи, `predict_next_day`/`predict_price_band` получат битый pickle и упадут в проде на следующем запросе. Исправлено на паттерн temp-file + `os.replace()` (атомарно на POSIX) — helper-функции `_atomic_pickle_dump`/`_atomic_json_dump` в `ml_pipeline.py`, применены ко всем 6 `.pkl` и обоим `.json`. Заодно добавлена метка `trained_at` (ISO UTC) в `metrics_report.json`/`conformal_calibration.json` — раньше дата последнего обучения нигде не персистилась.
    - **Новая ночная автоджоба**: `run_nightly_model_retrain()` в `src/tasks/scheduler.py`, зарегистрирована `scheduler.add_job(..., 'cron', hour=2, minute=0, id='nightly_model_retrain')` — та же цепочка `sync_realtime_data`→`train_models`→`train_quantile_models`, обёрнута в `try/except` (не роняет процесс при сбое, старые `.pkl` остаются рабочими благодаря атомарности выше). 02:00 даёт ≈4 часа запаса до дневной джобы прогноза в 06:00.
    - Докстринг `POST /api/retrain` (`app.py`) обновлён — старое предупреждение про нехватку памяти снято (числа устарели), но добавлено новое: этот HTTP-эндпоинт синхронный и всё равно блокирует event loop FastAPI на время обучения (~132с) — для регулярного обучения использовать ночную джобу, эндпоинт оставлен только для разового ручного запуска вне пиковой нагрузки.
    - **Задеплоено на VPS** (hot-patch `docker cp`+`docker restart`, затем `docker compose build && up -d` для закрепления), atomic-write и `trained_at` перепроверены отдельным быстрым прогоном `train_quantile_models()` в уже пересобранном контейнере — `.tmp`-огрызков нет, `conformal_calibration.json` содержит `trained_at`. Закоммичено локально и запушено в `origin/main` (3 коммита — `app.py`, `scheduler.py`, `ml_pipeline.py`).

16. **(2026-08-06)** — по просьбе пользователя изучены официальные документы `oree.com.ua` по правилам рынка РДН/ВДР — подготовка к будущей реальной API-интеграции для автоматизации подачи заявок. **Чисто исследовательская сессия, код не менялся** — только документация (этот файл + MEMORY.md §8) и файл плана `/home/oleg/.claude/plans/sunny-doodling-rabbit.md` (не в git).
    - Прочитан полностью действующий документ «Правила ринку "на добу наперед" та ВДР» (100 стр., НКРЕКП №308/14.03.2018 в ред. №1169/24.06.2019, зі змінами по №832/02.05.2023) — скачан с `oree.com.ua/index.php/web/10648`, PDF→текст через `pypdf` (`pip3 install --user --break-system-packages pypdf`, т.к. в окружении нет poppler-utils и не работает `python3 -m venv`). На сайте также лежит устаревший «Проект від 20.10.2017» с похожим названием файла — не перепутать.
    - Ключевые факты: закриття воріт РДН — 12:00 доби, що передує постачанню; ВДР — подача з 15:00, закриття воріт за 60 хв до кожної години (ковзний дедлайн); ціни 10.00–50000.00 грн/МВт·год (не плутати з бізнес-`PRICE_CAP=16000.0` в `ml_pipeline.py`); обов'язкові КЕП + escrow-рахунок; тарифи 2026 року — 4669.71 грн/міс + 6.88 грн/МВт·год.
    - **Найдено принципиально важное**: ринок двосторонніх договорів (РДД, постанова №307) — це довгостроковий OTC-механізм для попереднього узгодження обсягів, а НЕ миттєвий fallback «заявка не зіграла → продай прямо зараз». Для fallback-режиму реалістичніший шлях — автоподача на ВДР (вже частково спроектовано як порада диспетчеру, `estimate_idm_price_for_hour`).
    - **Технічна платформа**: OREE використовує XMTRADE|PXS — та сама вендорська платформа XMtrade/ISOT, що й у словацького OKTE, де публічно задокументовано REST+WebSocket API (сертифікат+basic auth). У OREE публічної документації API не знайдено, але з огляду на спільного вендора приватний API, ймовірно, видається зареєстрованим учасникам за запитом.
    - **Решение пользователя**: доступа в кабинет OREE пока нет, автоматическая подача через API — дело будущего, не срочно. Сейчас и далее диспетчер продолжает вводить заявки вручную; программа не пассивна — «руководит процессом» (прогноз+MILP → анализ точности → готовое предложение через `generate_bids_for_date`), что уже реализовано. Roadmap на будущее (когда появится доступ) зафиксирован в MEMORY.md §8 и файле плана: абстракция `oree_client` (Mock/реальный клиент за фиче-флагом) → расширение `MarketBid` полями реальной подачи → реальная автоподача с dry-run по умолчанию → замена ВДР-совета на реальное исполнение.

17. **(2026-08-06, продолжение)** — по просьбе пользователя добавлена новая страница «Про програму» прямо в веб-приложение (не markdown-документация): пункт левого меню, открывается в основной области справа. Содержание — прямое следствие исследования из п.16: что за программа и на каких реальных данных считает прогноз (oree.com.ua РДН/ВДР, Open-Meteo, ENTSO-E, LightGBM+P10/P90), как считается MILP-план и что диспетчер может скорректировать вручную, честное описание ежедневного автоцикла (06:00 прогноз+план, 02:00 ночной ретрейн — с явной пометкой-предупреждением, что соответствие «06:00 контейнера» реальному київському часу не перепроверено), пошаговая инструкция диспетчеру с реальными дедлайнами (подача РДН до 12:00, звірка з фактом після ~13:00, ВДР — ковзний дедлайн за 60 хв до години), текущий статус реализации и roadmap автоматизации (тот же, что в п.16, включая явное уточнение, что РДД не используется как миттєвий fallback).
    - Новый файл `frontend/src/pages/shared/About.tsx` (чисто статична сторінка, без API-викликів); правки `frontend/src/layouts/AppShell.tsx` (пункт `SHARED_NAV` + `PAGE_TITLES`) и `frontend/src/App.tsx` (роут `/about` у спільному блоці `AppShell workspace="dispatcher"`) — видно з обох воркспейсів, роль не фільтрує пункти меню (немає такого прецеденту в проекті).
    - Перевірено `tsc -b --force` (єдина команда, що відповідає прод-збірці) — чисто, exit code 0.
    - **Задеплоєно на VPS**: `scp` трьох файлів → `docker compose build smartbess-platform && up -d` (повний ребілд, бо новий Vite-роут, hot-patch тут не підходить) → підтверджено `curl` по реально зібраному JS-бандлу (`grep -rl 'Про програму' /app/frontend/dist/assets/*.js` знайшов рядок) і `curl -o /dev/null -w '%{http_code}' /about` → `200`.

18. **(2026-08-06, продолжение)** — пользователь попросил спланировать конкретный следующий шаг по мотивам исследования OREE (п.16), но `oree_client`-абстракцию (реальную API-интеграцию, даже за Mock-заглушкой) явно отложил на будущее: «строить работу по АПИ будем позже». Вместо этого реализованы 4 улучшения, не требующие доступа к API OREE, основанные на уже изученных реальных правилах рынка — план см. в истории сессии, план-файл `/home/oleg/.claude/plans/sunny-doodling-rabbit.md`:
    - **(a) Границы цены заявки** (10.00–50000.00 грн/МВт·год, легальні межі OREE, окремі від бізнес-`PRICE_CAP=16000.0`) — `clamp_bid_price_to_oree_bounds` у `bidding_service/services.py`, застосовується в `generate_bids_for_date` після формули маржі; `bid_price_legally_clamped` обчислюється на льоту в `_bid_to_dict` (без нової колонки БД); агрегат `n_price_clamped` у відповіді `/bids/generate`; UI-попередження (`AlertTriangle`) в таблиці заявок `OptimizationSchedule.tsx`.
    - **(b) Реальні тарифи учасника ринку OREE у фінмоделі** (4669.71 грн/міс + 6.88 грн/МВт·год, тариф 2026) — `TariffService.calculate_oree_participation_fee_uah`; `ReportingService.calculate_project_economics` отримав опціональний `oree_participation_fee_uah` (не змішується мовчки з generic opex); `get_executive_summary_report` тепер накопичує реальний `traded_volume_mwh` по кожному дню кешу (self-heal бэкфилл старих записів кешу) і віднімає реальну щоденну комісію OREE у траєкторії payback. Фронтенд: нова строка в `RoiPayback.tsx` (client-side калькулятор) і нова KPI-картка «Внесок OREE YTD» в `ExecutiveOverview.tsx`. Рішення: додано ЯК ОКРЕМИЙ рядок, не замінює існуючу евристику `2%×CAPEX+180000` (різна природа витрат).
    - **(c) Live countdown до закриття воріт РДН/ВДР** — новий компонент `frontend/src/components/BidGateCountdown.tsx`, чисто клієнтська `Date`-арифметика через `Intl.DateTimeFormat(timeZone: 'Europe/Kyiv')` (перший timezone-aware код у фронтенді, свідомий вибір — правила OREE задані в київському часі, незалежно від таймзони браузера диспетчера). Рендериться на `OptimizationSchedule.tsx` над карткою «Заявка РДН».
    - **(d) «Центр дій» диспетчера + Telegram-нагадування** — нова функція `build_daily_action_summary(db, asset, target_date)` в `bidding_service/services.py` (ТІЛЬКИ читає існуючі `MarketBid`, нічого не генерує/не звіряє сама — щоб не перетворитись на приховану автоматизацію, явно відкладену користувачем); новий read-only ендпоінт `GET /api/v1/bids/action-summary`; новий компонент `frontend/src/components/BidActionCenter.tsx` (перевикористовує amber/green/blue-ідіому з `PriceForecast.tsx`/`GridRiskPanel.tsx`), рендериться першим на `OptimizationSchedule.tsx`. Telegram: `check_and_send_bid_reminder()` в `telegram_bot.py` за зразком `check_and_send_grid_stress_alert()` (та сама dedup-механіка через `data/external_cache/telegram_alerts_sent.json`, але дедуп-ключ — за ЗМІСТОМ (`hash` списку дій), а не лише за датою, бо список дій може легітимно змінитись протягом дня). Нова окрема джоба `run_bid_reminder_check` у `scheduler.py` о 10:00 (~2 год запасу до 12:00 воріт РДН; та сама незалежна від "06:00 контейнера = київський час" застереження). Новий прапорець `bid_reminder_telegram_enabled` (default `true`) в `SystemSettingsModel`/`system_settings.json`, тумблер у `Settings.tsx`.
    - **Ніяких нових таблиць/колонок БД** — усі нові «поля» обчислюються на льоту або в JSON-кеші з self-heal бэкфиллом (узгоджено з тим, що `Base.metadata.create_all` не робить ALTER).
    - **Тести**: новий `test_bidding_service.py` (clamp × 3, `build_daily_action_summary` × 2, через реальний `SessionLocal`+синтетичну дату `2099-01-01/02`), розширено `test_services.py` (OREE fee × 2). Локально виявлено і виправлено: локальна sqlite (`data/smartbess.db`) ніколи не проганяла `init_db()` після додавання `MarketBid`/`BidMarginOverride`/`PriceShiftOverride` (ці таблиці розроблялись одразу на VPS) — довантажено через `Base.metadata.create_all` (безпечно, не чіпає існуючі таблиці/дані), але зауваження: `init_db.py` сам не імпортує `src.database.models`, тому для будь-якого standalone-скрипта потрібен явний `import src.database.models` ПЕРЕД викликом `init_db()`, інакше нові ORM-класи не зареєстровані в `Base.metadata` і create_all їх не бачить.
    - **Перевірено `tsc -b --force`** — одна помилка знайдена й виправлена: `lucide-react`-іконки не приймають `title`-проп напряму (обгорнуто в `<span title="...">`). Після фіксу — чисто, exit code 0.
    - **Перевірено локальним HTTP-циклом** (порт 5050, реальний Asset+ChargeDischargePlan+PriceForecast на 2026-07-13): `/bids/generate`→`/bids/action-summary`→`/bids/settle`→`/bids/action-summary` знову (`all_settled: true`, action `severity: "ok"`) → `/reports/executive-summary` (`oree_participation_fee_ytd_uah: 36226.95` за 217 днів, self-heal бэкфилл відпрацював) → `POST /optimization/settings` з новим прапорцем підтверджено збережено/прочитано. `check_and_send_bid_reminder()` коректно повертає `{"sent": false, "reason": "TELEGRAM_CHAT_ID не налаштований"}` (локально `TELEGRAM_CHAT_ID` не задано — очікувано, `TELEGRAM_BOT_TOKEN` є).
    - **Задеплоєно на VPS**: `scp` 18 файлів → `docker compose build && up -d` (повний ребілд). Підтверджено реальними запитами: JS-бандл містить `Що робити зараз`/`Внесок OREE`/`Реєстраційний внесок OREE`/`закриття воріт`; `GET /optimization/settings` повернув `bid_reminder_telegram_enabled: true` за замовчуванням; `GET /bids/action-summary` на дату без заявок повернув коректну підказку; `GET /reports/executive-summary` на реальному проді (217 днів реальних даних) — перший запит 30.2с (self-heal бэкфилл `traded_volume_mwh` для всіх старих днів кешу), повторний — 0.76с (кеш прижився), `oree_participation_fee_ytd_uah: 43442.64`. Логи контейнера чисті (єдине повідомлення — відоме й не пов'язане `SCADA Error: Could not connect... 127.0.0.1:5020`).

19. **(2026-08-21)** — пользователь прислал внешнее code review (`CODE_REVIEW.md`/`message.txt`, ревью коммита `b4f3f8d`, 20 замечаний). Между п.18 и этой записью хронология не обновлялась — редизайн фронтенда 2026-08-07 (design-токены, Plus Jakarta Sans, зебра-таблицы, анимации, mobile-адаптивность, `impeccable`-критика P0-P5) и точечная правка через `humanizer-ru` в тот же день закоммичены (`230c9f6`…`f4800c5`), но не задокументированы здесь — при потере нити сверяться с `git log`, не только с этим файлом.
    - Также перед этой сессией обнаружено и устранено **расхождение git-историй**: локальный `main` и `origin/main` разошлись (10 vs 20 коммитов, содержимое идентично за вычетом словоформ) — та же причина, что в п.12 (пуш через `mcp__github__push_files` создаёт другие SHA). Исправлено обычным `git merge origin/main` (2 тривиальных конфликта в `OptimizationSchedule.tsx`, решены в пользу более свежей формулировки), запушено. **Заменена схема push**: локальный SSH-ключ (`~/.ssh/id_ed25519.pub`) зарегистрирован в GitHub-аккаунте пользователя, `origin` переключён на `git@github.com:Olegkg2018/smartbess-ems.git` — обычный `git push`/`git fetch` теперь работает без токенов/MCP.
    - Пользователь выбрал начать доработку с «Этапа 1» ревью (пункты 1-4: mock-auth forgery, открытые Postgres/Redis, симулированная SCADA как факт, фиктивный Four-Eyes байпас) — самые опасные и не требующие новой архитектуры данных. Пункты 5-20 (энергия×60 в отчёте, settlement без проверки SoC, lineage-версионирование расчётов, единая модель времени, тесты) — НЕ начаты, следующие этапы.
    - **(a) Mock-auth forgery закрыта частично, не «настоящим OIDC»** — в проекте нет задеплоенного Keycloak/IdP (`KEYCLOAK_URL` смотрит на `localhost:8080`, нигде не поднят), поэтому реализовать настоящий OIDC-flow невозможно без внешней инфраструктуры. Вместо этого: токен теперь подписывает только backend (`POST /auth/mock-login`, `MOCK_JWT_SECRET` из `Settings`, новый файл `src/api/v1/endpoints/auth.py`) — раньше фронтенд сам собирал `alg:'none'` токен с произвольной ролью в claims (`client.ts::getMockToken`), а `security.py` при неудаче проверки подписи полностью отключал её (`verify_signature: False`) — эту fallback-ветку убрали целиком. `client.ts` теперь асинхронно получает и кеширует (10 мин TTL) подписанный токен через новую `getAuthToken()` — единственная точка изменения, ~40 мест вызова `api.xxx(activeRole,...)` не тронуты. При старте приложения (`app.py` lifespan) — громкий `print`-warning, если `OIDC_MOCK_MODE=True`. **Осталось forgeable**: сам `/auth/mock-login` не спрашивает пароль (canned demo-акаунт на роль) — это сознательный компромисс без реального IdP, не «безопасно для мультитенантного продакшена».
    - **(b) Postgres/Redis** — та же уязвимость, что уже привела к ransomware-инциденту 2026-07-15 (см. ниже). `docker-compose.yml`: `POSTGRES_PASSWORD`/`REDIS_PASSWORD` обязательны через `.env` (`:?` — падает явно, если не задано), порты `127.0.0.1:5432`/`127.0.0.1:6379` вместо `0.0.0.0`. `.env.example` дополнен плейсхолдерами.
    - **Побочно найден и исправлен реальный баг**: `.env` копируется в образ (`Dockerfile: COPY . .`), `pydantic-settings` (`config.py::Settings`) по умолчанию падает на «лишних» переменных .env — добавление `POSTGRES_PASSWORD`/`REDIS_PASSWORD` в `.env` уронило старт контейнера (`ValidationError: extra_forbidden`) при первой пересборке. Исправлено `Config.extra = "ignore"`.
    - **(c) Честная SCADA-телеметрия** — `AssetDetail.tsx` рисовал литералы (`20.0%`, `-150.0 кВт`, `24.8°C`, `99.85%`), `AppShell.tsx` — статичный бейдж `ONLINE`, реальная `BessTelemetry` писалась, но нигде не читалась. Новый `GET /assets/{id}/scada-status` (`assets.py`, честный `connected` по свежести записи <120с) + `scadaStatus` в `AppContext.tsx` (опрос раз в 20с) + `AssetDetail.tsx`/`AppShell.tsx` теперь показывают реальные значения и freshness. Новый флаг `SCADA_SIMULATOR_ENABLED` (`config.py`, default `True` — единственный реально существующий режим, реального Modbus-оборудования не подключено).
    - **(d) Четырёхглазый (Four-Eyes) демо-байпас на Asset Detail честно помечен как демо** — раньше кнопки всегда слали жёсткие 150 кВт (значение из инпута игнорировалось), `ApprovalModal` требовал «секретний криптографічний ключ» (проверялась только непустая строка), а `executeApprovedAction` не делало НИ ОДНОГО вызова API, но показывало `alert('Команду успішно надіслано до BESS контролера Modbus TCP!')`. Полный рабочий Four-Eyes (новая таблица команд, реальный второй подтверждающий, `poll_bess_and_control()` читает её) — по объёму отдельный этап, НЕ реализован. Сейчас: input стал контролируемым, формулировки честные («демо, не підключено до BESS»), финальный alert — честный. **Важно не путать** с уже реальным и подключённым к БД `ManualOverride`/«Ручна потужність» на Optimization Schedule — та фича работает по-настоящему (хоть `poll_bess_and_control()` тоже её не читает — это отдельный, не исправленный в этой сессии пробел, см. `CODE_REVIEW.md` п.4 полностью).
    - **Проверено на реальном локальном docker-compose** (Postgres+Redis+platform, пересобрано с `-v` для чистой инициализации новых паролей): `POST /auth/mock-login` → подписанный токен → 200 на защищённых эндпоинтах; старый `alg:none` forged-токен → 401; `redis-cli ping` без пароля → `NOAUTH`; `ss -ltn` подтвердил `127.0.0.1`-only на 5432/6379; `GET /assets/{id}/scada-status` вернул реальные данные симулятора; JS-бандл (`docker exec ... grep`) содержит новые честные строки UI и НЕ содержит старую ложь про криптографический ключ. `tsc -b --force` чисто. Headless-браузер (Playwright) не смог запуститься в этом WSL-окружении (`libnspr4.so` отсутствует) — тот же класс ограничения, что и с `npm run dev` (см. выше), проверка UI сделана через grep по собранному бандлу вместо скриншота.
    - **Задеплоено на VPS (2026-08-21, тем же вечером)**: живой Postgres на VPS уже имел реально ротированный пароль ещё с ремедиации ransomware-инцидента (2026-07-20) — просто захардкоженный прямо в `docker-compose.yml`, а не через `.env`. `ALTER USER` не понадобился: значение перенесено в `.env` (`POSTGRES_PASSWORD=` — тот же самый пароль) без изменения, поэтому `smartbess-db` при `docker compose up -d` даже не пересоздавался («Running», не «Recreate»). `REDIS_PASSWORD` сгенерирован новый (Redis раньше не имел пароля вообще). **VPS-версия `docker-compose.yml` НЕ копия локальной** — на VPS `smartbess-db`/`smartbess-redis` вообще без `ports:` (строже, чем локальный `127.0.0.1:...` для sandbox-дебага), это сохранено. Деплой файлов — не `git pull` (git HEAD на VPS застрял на `ab2f612`, глубоко разошёлся с историей, working tree годами живёт на scp-хотпатчах без коммитов — чинить это отдельная большая задача, не в рамках этого этапа), а точечный `scp` только изменённых файлов, построчно сверенных с локальными (см. `reference_vps_deployment.md`).
    - **Найдена и исправлена реальная ловушка**: `docker-compose.override.yml` (VPS-only, не в git, задаёт `mem_limit`/tuning) для `smartbess-redis` тоже содержит свой `command:` (`--maxmemory 48mb --maxmemory-policy allkeys-lru`) — Compose не мёрджит `command:` между файлами, override ПОЛНОСТЬЮ заменяет его, тихо отбросив `--requirepass` из базового `docker-compose.yml`. Обнаружено только повторной живой проверкой (`redis-cli ping` без пароля неожиданно вернул `PONG`) — не поверили единственной проверке автосборки. Исправлено переносом `--requirepass ${REDIS_PASSWORD}` в сам override-файл, с комментарием-предостережением на будущее.
    - **Проверено на публичном `http://olegkg2016.vps.ho.ua:5004` реальными запросами**: `/auth/mock-login` → 200, старый forged-токен → 401, `/assets/{id}/scada-status` вернул реальные данные симулятора, `/reports/executive-summary` (упражняет путь через Redis-кэш) → 200 — подтверждает, что приложение реально аутентифицируется в Redis новым паролем, не только что контейнер поднялся.
    - Пользователь также прислал отдельное, более глубокое ML-методологическое ревью (`data leakage` в `prepare_features()`, backtest не повторяет production inference, погода без привязки к target-дате/timezone, P10/P90-калибровка теряет гарантию после переобучения на всех данных и т.д.) — **зафиксировано как следующий этап, не начато**: `docs/review_ml_forecast_pipeline_2026-08-21.md`. Главный вывод оттуда: исторические WAPE-цифры в MEMORY.md (обоснование решений по фичам/ensemble/lag) измерялись на алгоритме, не совпадающем с production inference, и с утечкой в target — это не значит, что решения были неверны, но офлайн-доказательства под ними слабее, чем казалось.

20. **(2026-08-21, продолжение сессии)** — пользователь попросил начать следующий этап: отдельное, более глубокое ML-методологическое ревью (`docs/review_ml_forecast_pipeline_2026-08-21.md`, см. п.19) — data leakage в `prepare_features`, backtest не повторяет production inference, погода без привязки к target-дате/timezone, P10/P90-калибровка теряет гарантию. Три параллельных Explore-агента перепроверили все находки построчно — подтверждены, местами хуже (напр. `forecast_accuracy.py::compute_rolling_accuracy` docstring обещал фильтр по времени генерации, а код такого фильтра не имел вообще; `walk_forward_backtest`/`quantile_coverage_backtest` не вызываются НИГДЕ в приложении — только вручную).
    - Четвёртый агент (Plan) спроектировал реализацию, разбив «что исправить сначала» на **Фазу A** (аддитивно, нулевой риск для текущих предсказаний) и **Фазу B** (реально переписывает feature builder + backtest — отдельная сессия). Пользователь подтвердил: Фаза A сейчас, Фаза B отдельно; таймзоны править **только вперёд** (историю, включая реальные `realized_profit_uah`, не пересчитывать); будущий backtest на архивной погоде должен требовать обязательный `acknowledge_approximation=True`.
    - **Реализовано (Фаза A)**: явные таймзоны (OREE Kyiv→UTC при приёме, только новые данные; Open-Meteo `&timezone=UTC` — проверено живым запросом, совпадает с прежним дефолтом, реального пересчёта нет; новый `src/core/time_utils.py::assert_naive_utc` как защита от регрессии на границе каждого источника); неизменяемая история `ForecastRun`/`ForecastRunHour` (`models.py`, новый `forecast_persistence.py`) — пишется ДОПОЛНИТЕЛЬНО к `PriceForecast` (её контракт не менялся); `compute_rolling_accuracy` теперь реально фильтрует по `generated_at_utc < timestamp`, с фолбэком на старый путь для дат до этой фичи; `WeatherForecastArchive` — архивирует реально использованную прогнозную погоду (включая честную маркировку `synthetic_fallback`) для будущего честного backtest.
    - **Проверено локально реальным HTTP-циклом**: новые таблицы создаются через `create_all()` без ошибок; `POST /forecast/run` пишет идентичный `PriceForecast` и новую `ForecastRun`/`ForecastRunHour`; повторный запуск создаёт отдельную запись истории (не перезаписывает); архив погоды реально заполняется (`source: openweathermap`). **Ключевая живая проверка**: ретроактивный прогноз на прошлую дату (сгенерирован сегодня для 2026-07-13) корректно провалился в фолбэк (`n_hours_from_forecast_run_history: 0`) — именно то поведение, которое не даёт «улучшить» метрику задним числом.
    - Закоммичено (`6e395e1`, `1e27fff`), запушено в `origin/main`. **Задеплоено на VPS тем же вечером** (точечный `scp` 8 файлов, побайтово сверено, `docker compose up -d --build smartbess-platform` — БД/Redis не пересоздавались). Подтверждено реальными запросами к `http://olegkg2016.vps.ho.ua:5004`: новые таблицы созданы в живом Postgres (`\dt` подтвердил), `POST /forecast/run` на сегодняшнюю дату реально записал `ForecastRun` с `trigger='manual_api'` и настоящим `generated_at_utc`, `WeatherForecastArchive` заполнился (24 строки, `source='openweathermap'`), `GET /reports/forecast-accuracy` отработал без регрессии (`n_hours: 727`, фолбэк на старый путь — ожидаемо, `ForecastRun`-истории для прошлых дат ещё нет).
21. **(2026-08-21, продолжение сессии)** — пользователь попросил начать Фазу B того же ML-ревью (см. п.20): единый as-of feature builder, устранение утечки, переписывание `walk_forward_backtest` на вызов production-пайплайна, фикс калибровки P10/P90. Свежий Explore-агент перепроверил точные текущие номера строк (Фаза A не трогала `ml_pipeline.py`, всё совпало), затем Plan-агент детально спроектировал реализацию.
    - **Ключевой архитектурный момент**: просто вызвать `build_forecast_feature_matrix` для исторической даты недостаточно — CSV сегодня уже «знает» опубликованные значения IDM/ENTSO-E за тот исторический день (публикация давно случилась), тогда как прод в реальный момент их не видел. Решение: новый параметр `as_of` — хвіст `hist_before_target` ближче за `IDM_PUBLICATION_DELAY_HOURS=24`/`ENTSOE_PUBLICATION_DELAY_HOURS=6` годин до `as_of` явно затирається в NaN ПЕРЕД тим же відновленням форми хвоста, що вже давно перевірено в проді. Для живого проду (`as_of=None`→`utcnow()`) — no-op.
    - Новий модуль `src/modules/forecast_service/feature_pipeline.py` — переніс сюди `FEATURES`, `build_training_table` (наступник `prepare_features`), `build_asof_feature_matrix` (наступник `build_forecast_feature_matrix`), `clip_and_shift` (раніше локальне замикання в `predict_next_day`, тепер спільне для проду й бектеста), `_get_generation_adjustment`/`_get_reference_capacities_mw`/`_get_baseload_passthrough_ratio`/`_get_price_shift_pct`. `ml_pipeline.py` схуд на ~290 рядків нетто (706 рядків торкнуто), старі назви лишились тонкими сумісними обгортками.
    - **Утечка усунена**: `interpolate(method='linear')` (навіть без явного `.bfill()`) для ВНУТРІШНІХ дірок все одно дивиться в обидва боки — теж утечка, не лише колонки з явним `.bfill()`. Замінено на `ffill(limit=6)` — лише минулим значенням; target `Price` взагалі не заповнюється (чесний NaN → рядок відсіюється). На реальних даних: 49411 → 48965 рядків (~0.9% відсіяно чесно).
    - **`walk_forward_backtest` тепер реально викликає `build_forecast_feature_matrix`** (ту саму функцію, що й `predict_next_day`/`predict_price_band`) для конфігурації `model_type='lightgbm'` без `use_surplus_classifier`/`extra_features` (єдина, що має прод-еквівалент) — `summary['methodology_version']` відрізняє новий шлях (`phase_b_asof_unified_2026`) від старого зрізового для ensemble/surplus/extra_features режимів (`phase_b_leakage_fix_only_2026`, лише утечка виправлена). Нові параметри `weather_mode`/`acknowledge_approximation=False` (обов'язковий для `'archived_actual_approx'`, інакше `ValueError` — рішення користувача) і `weather_mode='archived_forecast'` (з нового `WeatherForecastArchive`, Фаза A) — поки майже завжди пропускає дні (архів лише почав накопичуватись), чесно лічить `skipped_days_no_weather_archive`, не підміняє мовчки.
    - **Калібрування P10/P90 виправлено**: `train_quantile_models` більше не перенавчає фінальні моделі на всіх даних — продові `.pkl` тепер САМЕ ті `lower_model`/`upper_model`, на яких порахована conformal-поправка (чесно навчені лише на `df_train`, без `df_calib`). Формальна split-conformal гарантія тепер реально виконується для розгорнутої моделі.
    - `quantile_coverage_backtest` (немає жодного виклику в застосунку) торкнуто мінімально — той самий gate + `build_training_table`, без as-of-уніфікації (нема продового шляху, з яким виправляти).
    - **Перевірено локально реальними прогонами** (не лише синтаксис): `build_training_table` на 49411 реальних рядках — 0 NaN; `walk_forward_backtest(test_days=5, acknowledge_approximation=True)` — повний прохід, новий `methodology_version`, 24/24 годин щодня; `predict_next_day`/`predict_price_band` на реальну завтрашню дату — санні числа, `lower<=upper`; `train_quantile_models()` — `coverage_after_conformal_correction: 0.8007` (≈ target 0.8); повний `train_models()` — 499с, WAPE 22.57% (в межах задокументованого діапазону, нічого не зламано).
    - **Не зроблено в цій сесії**: повний `walk_forward_backtest(test_days=90)` для статистично значущого порівняння зі старим базлайном (5-денний тест — лише перевірка механізму); повторна перевірка ~4 підозрілих історичних рішень (Lag_48, EU_DAM_Price, PL/RO погода, ensemble) на новому чесному бектесті — Фаза C, окрема сесія.
    - **Задеплоєно на VPS тим самим вечором**: точечний `scp` 2 файлів (`feature_pipeline.py` новий, `ml_pipeline.py`), побайтово звірено, `docker compose up -d --build smartbess-platform` (БД/Redis не пересозданы). Чистий старт, жодної помилки імпорту. Підтверджено реальним циклом на `http://olegkg2016.vps.ho.ua:5004`: `POST /forecast/run` на завтрашню дату реально відпрацював через новий шлях. **Живий `walk_forward_backtest(test_days=3, acknowledge_approximation=True)` запущено прямо в проді** (`docker exec`) — `methodology_version: phase_b_asof_unified_2026`, пік пам'яті лише 203.6MiB/900MiB (22.6%) — суттєвий запас. Локально перенавчені `.pkl` (train_models()/train_quantile_models() з цієї сесії) на VPS НЕ копіювались навмисно — нічна джоба (02:00) сама переобучить моделі вже новим (виправленим) кодом на наступному циклі.

22. **(2026-08-21, продолжение сессии)** — пользователь попросил начать Фазу C того же ML-ревью: повторную проверку ~4 исторически отклонённых feature-решений на новом честном as-of бектесте (Фаза B) + статистически значимый прогон текущей прод-конфигурации.
    - Добавлен новый опциональный параметр `idm_lag_hours=24` (`build_training_table`/`build_asof_feature_matrix`/`walk_forward_backtest`/`build_forecast_feature_matrix` — везде дефолт 24, прод не зачеплено) — щоб чесно повторно перевірити Lag_48 через реальний as-of-шлях (`extra_features` такий swap не підтримує, лише додає колонки).
    - Новий `scratch/backtest_phase_c.py` прогнав 6 реальних бектестів (не лише mechanism-check): baseline `test_days=90` (нова офіційна цифра — `mean_wape=24.89%`, в межах задокументованого діапазону 22-26%), Lag_48 (`test_days=59`), EU_DAM_Price_Lag_24, PL/RO погода, `ensemble_average`, `ensemble_weighted` (усі `test_days=60`, той самий test_days що й оригінальні експерименти — для прямого порівняння). Ensemble-прогони найдовші (22.6хв/43.9хв — тренують 3 моделі на кожному ретрейні).
    - **Результати**: Lag_48 підтверджено відхилено, тепер ще переконливіше (гірше по ВСІХ трьох метриках, не лише last_7d/30d, як на старому leaky бектесті). EU_DAM_Price/PL-RO погода лишаються відхиленими — паттерн "last_7d гірше" зберігся й після усунення утечки (аргумент, що знахідка була реальною). Ensemble — найцікавіший результат: last_30d покращився на 17% для ОБОХ схем зважування (тепер майже ідентичні, на відміну від оригінального прогону) — не підключено до проду (потрібна реальна ensemble-inference логіка в `predict_next_day`, не лише прапорець бектеста), але тепер серйозний кандидат для окремої майбутньої сесії.
    - `use_surplus_classifier`/recency weighting НЕ перепроверено (не вистачило часу сесії).
    - Оновлено `docs/review_ml_forecast_pipeline_2026-08-21.md` (новий розділ «Фаза C») та `MEMORY.md` §4/§6a — стара таблиця відхилених рішень доповнена новими честними цифрами, з явним маркуванням "[старий leaky бектест]" vs "перепроверено 2026-08-21".
    - Жодних змін до продової поведінки (усі нові параметри мають дефолти, що зберігають старий вихід) — FEATURES/модель НЕ змінено.

23. **(2026-08-22)** — пользователь попросил продолжить с оставшихся «критических» пунктов первого `CODE_REVIEW.md` — п.5 (завышение энергии из телеметрии ×60) и п.6 (settlement заявок без проверки физической достижимости через SoC). Оба напрямую искажают инвестор-видимые цифры Director Dashboard (Executive Overview / ROI & Payback), как и ML-ревью раньше.
    - **П.5 исправлен** (`reporting_service/services.py`, ветка `BessTelemetry` в `get_executive_summary_report`): раньше сумировался `power_kw*price_kwh` по каждому рядку телеметрии (~1 запись/хвилину) без множення на реальний Δt — завищення ~×60. Тепер інтегрується по РЕАЛЬНОМУ Δt між послідовними записами, з `MAX_TELEMETRY_GAP_HOURS=5/60` — якщо SCADA мовчала довше, пробіл чесно НЕ екстраполюється (не вигадуємо потужність за невиміряний час). `ManualOverride`-гілка (1 запис/годину за побудовою) не була баговою і не чіпалась.
    - **П.6 исправлен**: нова аддитивна таблиця `MarketBidSocFeasibility` (`models.py`, той самий паттерн, що `ForecastRun` у Фазі A — `create_all()` рестартом). Нова функція `_replay_soc_feasibility` (`bidding_service/services.py`) — послідовний SoC-реплей виконаних заявок після існуючого погодинного циклу `settle_bids_for_date` (та сама рекурсія SoC, що вже перевірена в MILP `optimize_battery_schedule`, без повторного solve). `compute_real_profit_capture_ratio` (`forecast_accuracy.py`) тепер джойнить цю таблицю й чесно обнуляє `realized_profit_uah` за години, де `soc_feasible=False` (енергія фізично не доставлена — 0, НЕ вигаданий imbalance-штраф, якого свідомо нема в даних). Graceful fallback на старий шлях для дат без цієї таблиці (той самий патерн, що вже в `compute_rolling_accuracy`). `GET /bids` і фронтенд (`OptimizationSchedule.tsx` — іконка `AlertTriangle` в колонці статусу + позначка «не зараховано» в P&L) тепер показують `soc_feasible`, щоб диспетчер не плутав «зіграла на аукціоні» з «фізично доставлено».
    - **Перевірено локально реальними прогонами проти живого Postgres у docker (не лише синтаксис)**: (1) синтетична телеметрія 60×1хв по 100кВт розряду → `traded_volume_mwh=0.1` (чесні 100кВт·год, не 6000), виручка 900 грн при реальній історичній ціні CSV 9000 грн/МВт·год (точний збіг, стара формула дала б ×60=54000); (2) `_replay_soc_feasibility` напряму — година з виконаним продажем, що фізично неможливий по SoC, коректно позначена `soc_feasible=False`, стан SoC при цьому не змінюється; (3) `compute_real_profit_capture_ratio` на 3 синтетичних добах — доба з однією SoC-неможливою годиною дала прибуток на 500 грн менше, ніж наївна сума (точний збіг очікування); (4) реальний HTTP-цикл на живому завтрашньому MILP-плані (2026-08-23): `POST /bids/generate` → 24 заявки, `POST /bids/settle` (з підставленими синтетичними actual-цінами) → 24 рядки `MarketBidSocFeasibility` реально створені в БД, `GET /bids` віддає `soc_feasible`, `GET /reports/executive-summary` не падає. Уся тестова БД-Data (тестові asset/bids/soc-rows/prices) прибрана після перевірки — production-дані не торкнуто.
    - Фронтенд: `MarketBid.soc_feasible` доданий у тип (`api/client.ts`), typecheck (`tsc --noEmit`) чистий, production-збірка (`docker compose build`, vite всередині контейнера) пройшла без нових помилок, локальний контейнер перезібраний і перезапущений — нова бандл-хеш (`index-BVdbIGZ3.js`) підтверджена живою віддачею з `/`.
    - **Задеплоєно на VPS 2026-08-23** (разом із п.24, одним циклом — див. нижче) — підтверджено реальними запросами.

24. **(2026-08-23) Живий інцидент, знайдений диспетчером**: «на 23 число данные подтянулись 21 час и не попадают в прогноз!!!» — на графіку прогноз/факт диспетчер не бачив ЖОДНОЇ реальної ціни РДН за сьогодні, хоча торги за 21 з 24 годин доби вже реально відбулись.
    - **Root cause знайдено за ~10 хвилин** прямою перевіркою продової БД на VPS: `market_prices` реально мала 21 рядок за 2026-08-23 (00:00–20:00) — синхронізація не була зламана. Баг був у `GET /forecast/actual` (`forecast.py::get_actual_prices`) — ендпоінт вимагав РІВНО 24 рядки в БД (`if len(rows) == 24`), інакше падав у `available: false`, ховаючи ВСІ вже реальні дані, поки не прийде 24-та година. Той самий баг дублювався і в live-фолбеку на `oree.com.ua` (теж `if len(df_day) == 24`).
    - **Фронтенд мав той самий прихований баг ще й окремо**: навіть якби бекенд повертав часткові дані, `PriceForecast.tsx` індексував факт напряму по позиції в масиві (`actualPrices.actual_prices_uah[i]`, де `i` — номер години 0-23), а не по реальному переліку годин `hours[]` — тобто без фіксу фронтенда часткові дані або взагалі не показались (`hasActual` вимагав `length === 24`), або показались б зі зсувом.
    - **Фікс**: `get_actual_prices` тепер повертає РІВНО ту кількість годин, яка реально є в БД (`available: true, partial: true` коли < 24), з `hours` = реальний список годин (не завжди `0..23`). `ActualPrices` (`client.ts`) отримав нове поле `partial`. `PriceForecast.tsx` тепер будує `Map<hour, price>` і індексує по ньому, а не по позиції; WAPE-розрахунок і інсайт-текст тепер чесно рахують лише по годинах, де факт реально є, з приміткою «поки відомо N з 24 годин» під час часткової доби.
    - **Перевірено без жодної вигадки даних**: локальний dev-контейнер має ідентичний реальний частковий набір (21 година, той самий CSV-пайплайн) — використаний як є для тесту, нічого не підставлялось штучно. Регресія перевірена на: повній добі (24/24 → `partial: false`, як і раніше), добі без жодних даних (`available: false`, як і раніше).
    - **Задеплоєно на VPS того ж дня** разом з п.23 (одна пересборка образу, один рестарт `smartbess-platform`, БД/Redis не чіпались) — точечний `scp` 9 файлів (усі з п.23 + `forecast.py`/`client.ts`/`PriceForecast.tsx` цього пункту), побайтово звірено (`sha256sum`). Підтверджено ЖИВИМ запитом до публічного `http://olegkg2016.vps.ho.ua:5004/api/v1/forecast/actual?target_date=2026-08-23` — реально повернув `available: true, partial: true` з тими самими 21 реальною годиною, які раніше ховались. `GET /reports/executive-summary` — без регресії.
    - **Урок**: "все або нічого" перевірки (`== 24`) на даних, що надходять ПРОТЯГОМ доби, systematically ховають реальні дані, що вже є — цей самий патерн варто перевірити в інших місцях кодбази, де зустрічається `== 24` замість `> 0`/часткового рендеру (не перевірено системно в цій сесії, лише знайдений і виправлений один конкретний випадок).

## Открытые пункты на конец сессии 2026-08-21

- Пункты 1-4 ревью безопасности реализованы, закоммичены (`6c7ee69`, `667dfbb`), запушены в `origin/main` **и задеплоены на VPS** (см. п.19) — подтверждено реальными запросами к `http://olegkg2016.vps.ho.ua:5004`.
- **VPS git по-прежнему НЕ синхронизирован с origin/main** (HEAD на `ab2f612`, working tree — многолетний scp-хотпатч без коммитов) — деплой сделан точечным `scp`, не `git pull`. Полная синхронизация git-истории VPS — отдельная незапланированная задача, не делать её попутно без явного запроса пользователя (риск конфликтов на десятках файлов).
- **Фаза A ML-ревью (п.20) реализована, закоммичена и задеплоена на VPS** — подтверждено реальными запросами.
- **Фаза B ML-ревью (п.21) реализована, проверена локально и задеплоена на VPS** — подтверждено реальными запросами, включая живой `walk_forward_backtest` прямо в контейнере (22.6% памяти, есть запас).
- **Фаза C ML-ревью (п.22) реализована, проверена и задеплоена на VPS (2026-08-22).** Точечный `scp` 2 файлов, чистый старт, `idm_lag_hours` параметр подтверждён в контейнере. Заодно проверено: ночная джоба 02:00 в ту же ночь впервые отработала на коде Фазы B без ошибок (`trained_at: 2026-08-22T02:05:23Z`, WAPE 22.59% — совпадает с локальным прогоном).
- Следующий шаг по ML-ревью — возможная отдельная сессия по ensemble (реальная inference-логика в `predict_next_day`, если будет интерес) — не начата, не запланирована как обязательная.
- Нічна джоба на VPS (02:00) сьогодні вночі вперше переобучить модель уже виправленим (Фаза B) кодом — варто зверитись з `trained_at`/`GET /api/metrics` наступного ранку, чи пройшло без помилок.
- **Пункты 5-6 первого ревью (`CODE_REVIEW.md`) реализованы, проверены локально и задеплоены на VPS 2026-08-23** (см. п.23-24) — подтверждено реальными запросами к `http://olegkg2016.vps.ho.ua:5004`.
- **Инцидент с `GET /forecast/actual` (п.24) исправлен и задеплоен на VPS 2026-08-23** — диспетчер снова видит реальные частичные данные факта РДН в течение дня, не только после полной публикации всех 24 часов.
- Пункты 7-20 первого ревью (`CODE_REVIEW.md`) не начаты: отсутствие lineage между прогнозом/планом/заявками, единая модель времени (Kyiv vs UTC vs container tz), MILP-статус солвера игнорируется, VaR не на исполняемом графике, тарифные настройки не централизованы, пробелы в валидации API, ролевой гейтинг UI, доступность (accessibility), устаревшее отображение графика в UI, тесты не изолированы от рабочей БД.
- **Не проверено системно**: другие места в кодбазе с аналогичным паттерном "всё или ничего" (`== 24` вместо частичного рендера) кроме уже найденного и исправленного `forecast.py::get_actual_prices` — см. п.24, «Урок».
- Модель прогноза по-прежнему переобучается автоматически 02:00 на VPS (`run_nightly_model_retrain`, п.15) — этой сессии не касалось. **Важно**: локально в этой сессии реально запущены `train_models()`/`train_quantile_models()` — свежие `.pkl`/`conformal_calibration.json` лежат в `data/` (gitignored), не задеплоены на VPS, там останутся старые до следующего ночного цикла или ручного деплоя.

## Если нить разработки потеряна — с чего начать

1. Прочитать этот файл целиком и [MEMORY.md](MEMORY.md).
2. `git log --oneline` и `git status` — что реально закоммичено/незакоммичено локально.
3. Если предстоит работа с VPS — сначала прочитать auto-memory файлы, указанные в разделе «Деплой / VPS» выше (адрес/доступ могли измениться).
4. Перед тем как менять `FEATURES` в `ml_pipeline.py` — прогнать `walk_forward_backtest`, не полагаться на интуицию (см. MEMORY.md — уже минимум 4 гипотезы «казалось бы логичные» не подтвердились на реальных данных).
5. Перед тем как считать деплой на VPS завершённым — не доверять только диф-проверке, дёрнуть реальный эндпоинт конкретной фичи и проверить содержимое ответа.
